# REST API base URL the client calls to mint rooms (GET /getRoom)
REACT_APP_SERVER_URL=http://localhost:4000

# WebSocket URL the client connects to for gameplay (room id is appended as /<roomID>)
REACT_APP_WS_URL=ws://localhost:4000

